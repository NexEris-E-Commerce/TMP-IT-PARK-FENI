import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export const metadata = { title: "My Account" };

export default function Page() {
  return (
    <PlaceholderPage
      title="My Account"
      description="Your account dashboard — orders, addresses, wishlist and profile — is coming with user accounts in a later phase."
    />
  );
}
