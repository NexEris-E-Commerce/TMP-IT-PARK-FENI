import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export const metadata = { title: "Shopping Cart" };

export default function Page() {
  return (
    <PlaceholderPage
      title="Your Cart"
      description="The full cart and secure checkout — with delivery zones, Cash on Delivery and bKash / Nagad / Rocket — arrives in the next phase."
    />
  );
}
